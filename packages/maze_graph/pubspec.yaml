name: maze_graph
description: Demo 2 - the pahndev Type-Safe-Maze-Demo methodology (graph state, legal exits only) reproduced and scaled.
version: 0.1.0
publish_to: none
resolution: workspace
environment:
  sdk: ^3.11.0
dependencies:
  args: ^2.5.0
  http: ^1.2.0
  jev_common:
    path: ../jev_common
dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
